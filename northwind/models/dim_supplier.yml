version: 2
models:
  - name: dim_supplier
    description: Supplier Dimension. One row per supplier
    columns:
      - name: supplierkey
        description: The surrogate key of the supplier
        tests:
          - not_null
          - unique
      - name: supplierid
        description: The business key of the supplier
        tests:
          - not_null
          - unique